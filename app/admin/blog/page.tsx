'use client'

export default function AdminBlog() {
  return (
    <div style={{ padding: 40 }}>
      <h1>Admin Blog (temporarily disabled)</h1>
      <p>This page is disabled to unblock the build.</p>
    </div>
  )
}
